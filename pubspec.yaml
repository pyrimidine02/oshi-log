name: girlsbandtabi_app
description: "girlsbandtabi_app"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.0.2+1
environment:
  sdk: '>=3.8.1 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # UI & Navigation
  cupertino_icons: ^1.0.8
  go_router: ^14.3.0
  cached_network_image: ^3.3.1
  url_launcher: ^6.3.0
  
  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  
  # HTTP & GraphQL
  dio: ^5.4.3+1
  http: ^1.5.0
  http_parser: ^4.1.2
  
  # Authentication & Storage
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.2.3
  
  # Location Services
  geolocator: ^12.0.0
  permission_handler: ^11.3.1
  
  # Map & Location UI
  
  # JSON Serialization
  json_annotation: ^4.9.0
  
  # Utility
  intl: ^0.20.2
  path: ^1.9.0
  connectivity_plus: ^6.1.0
  jose: ^0.3.5+2
  image_picker: ^1.1.2
  image_cropper: ^9.0.0
  file_picker: ^8.0.0
  flutter_image_compress: ^2.3.0
  gal: ^2.3.0
  flutter_local_notifications: ^19.4.2
  firebase_core: ^3.15.2
  firebase_messaging: ^15.2.10
  firebase_analytics: ^11.6.0
  google_mobile_ads: ^6.0.0

  # Map & Location UI
  google_maps_flutter: ^2.14.0
  apple_maps_flutter: ^1.4.0
  device_info_plus: ^12.3.0
  package_info_plus: ^8.3.0
  crypto: ^3.0.3
  firebase_crashlytics: ^4.3.7
  sentry_flutter: ^8.14.0
  uuid: ^4.5.1
  focus_detector: ^2.0.1
  app_links: ^6.0.0
  android_id: ^0.4.0
  flutter_timezone: ^4.1.1

  # Social Login SDKs / 소셜 로그인 SDK
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.4
  crop_your_image: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  
  # Testing Core
  mocktail: ^1.0.3
  integration_test:
    sdk: flutter
  
  # Performance Testing
  flutter_driver:
    sdk: flutter
  
  # Golden Tests & UI Testing
  golden_toolkit: ^0.15.0
  network_image_mock: ^2.1.1
  mocktail_image_network: ^1.2.0
  
  # Widget Testing Utilities
  flutter_test_helpers: ^0.1.0
  
  # Coverage Analysis
  coverage: ^1.6.4
  
  # Code Generation
  build_runner: ^2.4.9
  riverpod_generator: ^2.4.2

dependency_overrides:
  image_cropper:
    path: third_party/image_cropper

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true
  # EN: SPM integration breaks on non-ASCII (Korean) paths — the resolver
  # percent-encodes "00_소스코드" and then fails to find pubspec.yaml.
  # Keep CocoaPods until the repo moves to an ASCII path or Flutter fixes it.
  # KO: SPM 통합이 비ASCII(한글) 경로에서 깨짐 — "00_소스코드"를 percent-
  # 인코딩한 뒤 pubspec.yaml을 찾지 못함. 리포를 ASCII 경로로 옮기거나
  # Flutter가 수정할 때까지 CocoaPods 유지.
  config:
    enable-swift-package-manager: false

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # EN: Bundle the custom font license alongside the font files.
  # KO: 커스텀 폰트 라이선스를 폰트 파일과 함께 번들에 포함합니다.
  assets:
    - assets/fonts/Pretendard-LICENSE.txt

  fonts:
    - family: Pretendard
      fonts:
        - asset: assets/fonts/Pretendard-Regular.otf
          weight: 400
        - asset: assets/fonts/Pretendard-Medium.otf
          weight: 500
        - asset: assets/fonts/Pretendard-SemiBold.otf
          weight: 600
        - asset: assets/fonts/Pretendard-Bold.otf
          weight: 700
        - asset: assets/fonts/Pretendard-ExtraBold.otf
          weight: 800
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
